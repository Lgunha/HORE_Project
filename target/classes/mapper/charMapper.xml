<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hotel.dao.IF_ChaDAO">
	<insert id="insert" parameterType="com.hotel.VO.characterVO">
		insert into character_hotel values(#{detailAddr_hotel_character},#{character})
	</insert>
	
	<insert id = "insert_count" parameterType="com.hotel.VO.characterVO">
		insert into character_user values(#{id_user_character},#{character},1)
	</insert>
	<update id="already_insert" parameterType="hashmap">
		update character_user set count_cha = count_cha+1 where character =#{character} and id_user_character =#{id_user_character}
	</update>
	
	<select id ="select_cha_hotel" parameterType="String" resultType="String">
		select character from character_hotel where detailAddr_hotel_character = #{detailAddr_hotel_character}
	</select>
	<select id ="select_cha_user" parameterType="String" resultType="String">
		select character from character_user where id_user_character = #{is_user_character}
	</select>
	
</mapper>